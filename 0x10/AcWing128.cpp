#include <bits/stdc++.h>

using namespace std;

const int N = 1000010;

char op;
int Q, x;
int stkl[N], stkr[N];

int main()
{
    ios::sync_with_stdio(false);
    cin.tie(0), cout.tie(0);
    cin >> Q;
    while (Q--)
    {
        cin >> op;
        if (op == 'I')
        {
            cin >> x;
        }
    }
    return 0;
}